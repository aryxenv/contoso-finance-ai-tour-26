{
  "name": "@contoso-finance/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"Type-only package — no tests\" && exit 0"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
